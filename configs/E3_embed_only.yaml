# E3: Embedding-only ablation (C2 isolated). PLAN.md §6.E3, §18 claim 2.
inherits: base.yaml

method: "oamvhfmd_embed_only"
model:
  variant: "overlap_aware"
  enable_overlap_embed: true
# Loss is selected by the driver from method/variant (no enable_overlap_md_loss flag in code).
# For E3 (embed-only), use --variant embed_only on script 02; the driver will use baseline_md_loss.

paths:
  out_dir: "./results/E3_embed_only"

sweep:
  n_views: [4]
  seeds:   [42, 1337, 2024]
