# Core ML — versions pinned to match MV-HFMD's environment
torch>=2.1,<2.7
torchvision>=0.16
timm==0.9.10            # NON-NEGOTIABLE — see PLAN.md §4.1
einops>=0.7

# Data + IO
numpy>=1.26,<2.0
pillow>=10.0
pandas>=2.0
pyyaml>=6.0

# Eval + analysis
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65

# Frozen oracles (DINOv2 via torch.hub; CLIP / SigLIP via transformers)
transformers>=4.40

# Optional dev
pytest>=7.4
black>=24.0
ruff>=0.4
